import express from 'express';

const router = express.Router();

// Aquí puedes agregar tus rutas de usuarios
// Por ejemplo:
// router.get('/', userController.getAllUsers);
// router.post('/', userController.createUser);

export default router;
